<% if @item.destroyed? %>
   $('#item-' +<%= @item.id %>).hide();
   $('.js-items-count').html("<%= pluralize(@item.user.item.count, 'item') %>");
 <% else %>
   $('#item-' +<%= @item.id %>).prepend("<div class='alert alert-danger'><%= flash[:error] %></div>");
 <% end %>